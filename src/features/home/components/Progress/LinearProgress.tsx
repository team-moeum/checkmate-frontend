import { View } from "react-native";

export const LinearProgress = ({
  start = 0,
  end = 100,
  delay = 100
}: {
  start?: number;
  end?: number;
  delay?: number;
}) => {
  return <View></View>;
};
